import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.tcasistencia.app',
  appName: 'TC-Asistencia',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
